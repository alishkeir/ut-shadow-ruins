using UnityEngine;


// this script is used to control the boss character 
// it will handle the boss movements and attacks
// will also be responsible for small flying bats that the boss will spawn to attack (dive, like a projectile and get destroyed) the player
public class BossController2D : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
