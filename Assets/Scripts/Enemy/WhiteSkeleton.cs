using UnityEngine;



// first enemy in the game
// this is a skeleton with a sword
// this script will control the movement and attack of the skeleton
// it will also detect the player and chase them
public class WhiteSkeleton : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
