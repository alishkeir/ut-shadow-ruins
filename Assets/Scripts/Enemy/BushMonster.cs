using UnityEngine;


// third enemy in the game
// this is a bush monster
// this script will control the movement and attack of the bush monster
// it will also detect the player and chase them
public class BushMonster : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
